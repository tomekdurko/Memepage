import React from 'react';
import ReactDOM from 'react-dom';

const App = (props) => {
    return (
        <MuiThemeProvider theme={theme}>
          
        </MuiThemeProvider>
   
  );
}; 

export default App;